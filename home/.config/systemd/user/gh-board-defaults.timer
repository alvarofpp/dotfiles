[Unit]
Description=Roda o gh-board-defaults a cada 10 min

[Timer]
OnCalendar=*:0/10
# Pega o que passou enquanto o notebook estava desligado/suspenso.
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
