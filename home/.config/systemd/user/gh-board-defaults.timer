[Unit]
Description=Roda o gh-board-defaults a cada 15 min

[Timer]
OnCalendar=*:0/15
# Pega o que passou enquanto o notebook estava desligado/suspenso.
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
